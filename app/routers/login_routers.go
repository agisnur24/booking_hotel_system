package routers

/*import "database/sql"

func NewloginRouter(group *echo.Group, db *sql.DB) {
	loginRepository := loginrepository.NewLoginRepository()
	loginService := loginservice.NewLoginService(loginRepository, db)
	loginController := logincontroller.NewLoginController(loginService)

	group.POST("/login", loginController.Login)
}
*/
