package repository

/*
import (
	"context"
	"database/sql"
)

type LoginRepository interface {
	Login(db *sql.DB, login *domain.LoginDomain) (*domain.LoginDomain, error)
	CreateLogin(ctx context.Context, tx *sql.Tx, login *domain.LoginDomain) (*domain.LoginDomain, error)
}
*/
